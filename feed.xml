<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://luizabrs.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://luizabrs.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-12-15T21:14:22+00:00</updated><id>https://luizabrs.github.io/feed.xml</id><title type="html">Luiza Barros Reis Soezima</title><subtitle>Hi, I am Luiza Barros Reis Soezima and this is my personal website. </subtitle><entry><title type="html">Summing up - My experience with FLOSS</title><link href="https://luizabrs.github.io/blog/2024/next/" rel="alternate" type="text/html" title="Summing up - My experience with FLOSS"/><published>2024-06-25T07:14:00+00:00</published><updated>2024-06-25T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/next</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/next/"><![CDATA[<h1 id="its-floss-time-lets-get-down-to-it">It’s FLOSS time: let’s get down to it</h1> <p>This was my very first contact with open source concepts, philosophies and community. And I really liked it from the beginning.</p> <p>Before the expository classes about what is/who is it for/who does/why doing OpenSource, I had no idea about how huge and extense the graph of contributions were. And one the things that got me the most was the “Cathedral and Bazaar” model.</p> <p>The Cathedral model represents traditional, centralized development with controlled releases by a small group of developers. The Bazaar model epitomizes the open-source approach, featuring decentralized, collaborative contributions from a large community of developers.</p> <p>Basically the Bazaar model, exemplified by projects like Linux, Debian, etc leads to faster innovation and more robust software and also reaching more flexibility, transparency, and community engagement. On a world where we have so many different people thinking on the same issue, this is the perfect situation for reaching each time better solutions and intersections of solutions for better tools.</p> <h1 id="technical-limitations">Technical limitations</h1> <p>Another thing that I realized throughout the discipline is that if you have a perfect enviroment for development of the chosen project or issue, it is natural that the progress will come quicker and efficiently.</p> <p>But I was on the other side of this, and felt that all the time, all the classes, I was left behind because of my laptop, but knew that fixing my bugs and machine problems would also lead to a good learning process.</p> <p>I had to be patient and learn with late steps, and since we ended up grouping, I had to follow up with my groupmate laptop implementations. This made a bit harder to actually go down with testing, implementing my ideas or flowing with the development of the issue code itself.</p> <h1 id="grouping-and-submitting-is-cool-but-overheads-are-better">Grouping and submitting is cool, but overheads are better</h1> <p>During the grouping experience I personally found it very interesting to learn from Lincoln existent experience. Being on a group where one of the members is familiar with the needing enviroments and requisites always helps to follow up with the schedule and getting things done.</p> <p>On the other hand, I get the feeling that learning comes a lot from struggles and overheads. So, most of what I learned throughout the discipline was from the consequences of my problems during tutorials and other situations, rather than mainly from the contributions.</p> <p>I have never actually searched, executed, deleted and created so many stuff inside my operating system, so one of the things that I learned the most was about the OS itself and how it works on the real world side.</p> <h1 id="a-cool-new-project-to-follow">A cool new project to follow</h1> <p>After the entire knowledge obtained through the discipline, on the beginning of june I found out about a cool open-source project in which I would really like to contribute.</p> <p>The project is name <strong>OpenFHE</strong>, and it was an open-source project that focus on Fully Homomorphic Encryption (FHE) schemes. You can read more about it <a href="https://www.openfhe.org/">here</a>.</p> <p>The coolest thing about this project is that it diversifies and opens a lot on how you can contribute to the project, from solving issues, to creating new implementations and APIs as well as translating already existing implementations to another languages and experiment with them. You can learn more about it <a href="https://www.openfhe.org/downloads/">here</a>.</p> <p>My interesent landed on the <strong>C++</strong> implementation, which can be found <a href="https://github.com/openfheorg/openfhe-development">here</a>.</p> <p>For the contribution flow, you can find out more <a href="https://openfhe-development.readthedocs.io/en/latest/sphinx_rsts/contributing/contributing_workflow.html">here</a></p> <p>For the Crypto community this is amazing, especially considering that new implementations are always welcome in post-quantum side.</p> <p>Sadly, this dicovery was way too late considering the schedule of the discipline, and I couldn’t go through contributions on my on in this.</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about my personal experience with the FLOSS community and MAC0470 discipline]]></summary></entry><entry><title type="html">Contributing to kw(kernel workflow) - Part 3</title><link href="https://luizabrs.github.io/blog/2024/kw3/" rel="alternate" type="text/html" title="Contributing to kw(kernel workflow) - Part 3"/><published>2024-06-25T05:14:00+00:00</published><updated>2024-06-25T05:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/kw3</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/kw3/"><![CDATA[<h1 id="directions-for-the-second-phase">Directions for the second phase</h1> <p>For the second phase of the discipline, our group decided to keep contributing for the Kernel Workflow. After a first phase of a tense process dealing with the Linux Kernel, we found that going on this direction would be coold to learn but not a good idea if we actually wanted to deploy.</p> <p>When it comes to choosing open-source projects to contribute, we can list many famous ones like Debian, Kernel, KW, Python’s matplotlib, and etc. But considering the learning flow we had on the previous months, it was a good choice to keep contributing to KW, and possibily to understand more about how it works. Also, this was a good opportunity to be part of such a big project used by so many people. Linux is extensively used and contributing to is certainly life and career enhancing.</p> <p>Choosing a new issue to contribute is never something easy to do. It’s like picking the best potato out of a huge bag of potatos that all of them seems good, but when you peel them, err… it’s not exactly like that. In other words, at a first glance, reading the issue may seem doable, but once you go deep on the code and the dependencing most of the times you realize there is a lot more to be done and understood.</p> <p>Myself, I went over many issues and like <a href="https://github.com/kworkflow/kworkflow/issues/199">this</a>, seemed cool to be done, but the dependencies were huge (starting understanding issue#150 and use it as reference, as well as manipulating many files in order to enhance the test coverage). We also look up a bit at issues <a href="https://github.com/kworkflow/kworkflow/issues/120">120</a> and <a href="https://github.com/kworkflow/kworkflow/issues/468">468</a>.</p> <p>We ended up going for the issue <a href="https://github.com/kworkflow/kworkflow/issues/1049">1049</a> and <a href="https://github.com/kworkflow/kworkflow/issues/1051">1051</a>.</p> <h1 id="about-the-previous-isssue-we-were-doing---improving-kw-maintainers-test-coverage">About the previous isssue we were doing - Improving <code class="language-plaintext highlighter-rouge">kw maintainers</code> test coverage</h1> <p>The kworkflow maintainers currently lack tests for several critical corner cases where the feature is expected to fail. Introducing these tests would significantly enhance the test coverage, ensuring that the feature behaves as expected and reducing the likelihood of unintended changes in the future.</p> <p>We’ve opened a pull request addressing this issue directly <a href="kworkflow/pull/1118">here</a>.</p> <p>The concept itself is straightforward, but achieving acceptance on the first attempt can be challenging. We received suggestions from Rodrigo Siqueira and identified additional changes that could be made, particularly regarding an old and neglected test script.</p> <p><strong>Update:</strong> This contribution has been accepted, and the commit in the upstream project can be viewed <a href="https://github.com/kworkflow/kworkflow/commit/defa940391552e93eec6aab02f480111ced941a9">here</a>.</p> <h1 id="handle-trailer-a-new-issue"><code class="language-plaintext highlighter-rouge">handle-trailer</code> a new issue</h1> <p><strong>Managing Trailer Lines with KW</strong></p> <p>Both issues <a href="https://github.com/kworkflow/kworkflow/issues/1049">#1049</a> and <a href="https://github.com/kworkflow/kworkflow/issues/1051">#1051</a> address similar concerns regarding the manipulation of trailer lines in commits and patches. We’re aiming to introduce a new feature to streamline these tasks, making them easier and quicker.</p> <h2 id="what-is-git-interpret-trailer">What is <code class="language-plaintext highlighter-rouge">git-interpret-trailer</code></h2> <p>As mentioned on the official git documentation <a href="https://git-scm.com/docs/git-interpret-trailers">here</a>.</p> <p>In Git, a “trailer” is a specific kind of line found at the end of a commit message. Trailers are typically used to provide additional metadata about a commit, such as the author, reviewer, or references to related issues or tickets. They follow a standard format, making it easy to parse and process this metadata automatically.</p> <p>The format of a trailer line is usually:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>Key: value
</code></pre></div></div> <p>There are some common type of trailers:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">Signed-off-by</code></strong>: Indicates who has reviewed and approved the changes in the commit. Commonly used in projects that follow the Developer Certificate of Origin (DCO).</li> <li><strong><code class="language-plaintext highlighter-rouge">Co-authored-by</code></strong>: Indicates additional authors who contributed to the commit.</li> <li><strong><code class="language-plaintext highlighter-rouge">Reviewed-by</code></strong>: Specifies who reviewed the changes.</li> <li><strong><code class="language-plaintext highlighter-rouge">Fixes</code></strong>: References an issue that the commit fixes.</li> </ul> <p>The goal of the issue is to attack the following: Often a kernel developer has to perform some operations over commit or patch trailer lines. Adding tags such as ‘Reviewed-by’ or ‘Acked-by’ is a common task and sometimes it might be required to do this over multiple commits or patches. Also another common one is the ‘Fixes’ tag, that must follow a specific format.</p> <p>This feature helps on these cases. It uses ‘git rebase’, ‘git commit –amend’ and ‘git interpret-trailers’ as its backend in order to make these tasks quicker to complete.</p> <h2 id="the-current-state">The current state</h2> <p>Currently, we are still deliberating on the implementation approach. Should we create a new feature, such as <code class="language-plaintext highlighter-rouge">kw handle-trailer</code>, or integrate this functionality into an existing KW feature?</p> <p>We’ve opted for the first option and have opened a pull request that can be read <a href="https://github.com/kworkflow/kworkflow/pull/1121">here</a>.</p> <p>One of the addressed dicussions are about the name chosen for the issue fixing. Some suggestions we received include:</p> <ul> <li><code class="language-plaintext highlighter-rouge">kw annotate</code></li> <li><code class="language-plaintext highlighter-rouge">kw amend</code></li> <li><code class="language-plaintext highlighter-rouge">kw tag</code></li> <li><code class="language-plaintext highlighter-rouge">kw signature</code></li> </ul> <p>It’s too early to determine whether this PR will be accepted. This can be left for the future and you can keep up with the discussions updated on the <a href="https://github.com/kworkflow/kworkflow/pull/1121">Pull-Request</a></p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about our first contribution to kernel workflow]]></summary></entry><entry><title type="html">Contributing to kw(kernel workflow) - Part 2</title><link href="https://luizabrs.github.io/blog/2024/kw2/" rel="alternate" type="text/html" title="Contributing to kw(kernel workflow) - Part 2"/><published>2024-06-15T07:14:00+00:00</published><updated>2024-06-15T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/kw2</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/kw2/"><![CDATA[<h1 id="our-first-pull-request-for-kworkflow">Our First Pull Request for kworkflow</h1> <p>After receiving feedback and making adjustments, our first pull request for kworkflow was accepted. This blog post describes the process and lessons learned. You can find the pull request’s discussion <a href="https://github.com/kworkflow/kworkflow/pull/1100">here</a>.</p> <h2 id="issues-with-readability">Issues with Readability</h2> <p>The initial feedback we received pointed out some minor issues, such as poorly named files and specific case tests. These were not “feature-breaking” but highlighted the need for better readability and clarity in our code. At first, these issues seems to be all code-styling related and maturity in coding.</p> <p>One case to mention is the part of the code reponsible for capturing the content inside the <code class="language-plaintext highlighter-rouge">MODULE_AUTHOR</code> macro, trimming and formatting the content to be printed by the function.</p> <p>This block of code is actually a single line of commands piped one after another and is quite hard to understand what is happening. The suggestion was breaking this huge commands into a small set of separeted blocks and adding some comments to improve readbility and documentation of the changes and features.</p> <h2 id="understanding-and-modifying">Understanding and modifying</h2> <p>As we improved our pull request based on the suggestions, we questioned the purpose of the following code:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sed</span> <span class="nt">--expression</span> <span class="s1">':a'</span> <span class="nt">--expression</span> <span class="s1">'N'</span> <span class="nt">--expression</span> <span class="s1">'$!ba'</span>
</code></pre></div></div> <p>Despite our research, we couldn’t determine what this code did. We reviewed the sed documentation and the original commit when –authors was introduced but found no clear answers. Removing this code didn’t affect the output.</p> <p>Eventually, <a href="https://github.com/davidbtadokoro">@davidbtadokoro</a> explained that this code aimed to handle multi-line author statements, which was precisely what we were trying to address with our pull request. His detailed explanation is in the PR thread.</p> <h2 id="updating-our-pr">Updating Our PR</h2> <p>After receiving initial change requests, we added new commits to improve our code. The maintainers then asked us to squash these commits into one:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git rebase <span class="nt">--interactive</span> HEAD~4
</code></pre></div></div> <p>We selected:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    pick ...First Commit...
    squash ...Second Commit...
    squash ...Third Commit...
    squash ...Fourth Commit...
</code></pre></div></div> <p>This merged the new commits into a single one. We then updated our remote fork’s branch:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git push <span class="nt">--force-with-lease</span> origin issue69
</code></pre></div></div> <p>where issue69 is the branch we created for this issue.</p> <h2 id="final-touches-and-pr-resolution">Final touches and PR resolution</h2> <p>We enhanced our code’s readability by splitting it into sections and adding comments. We also removed the “unknown” code as it wasn’t necessary for our new function. Additionally, we created a new test file covering the multi-line use of MODULE_AUTHOR.</p> <p>There were further discussions on code style and best practices, but they are not crucial for this blog post. For full details, see the discussion <a href="https://github.com/kworkflow/kworkflow/pull/1100">here</a>.</p> <h1 id="changes-merged-into-kws-upstream">Changes merged into kw’s upstream</h1> <p>Our changes were accepted and merged into the kworkflow upstream. They were first merged into the unstable branch and will be periodically merged into the master branch.</p> <p>You can find our contribution <a href="https://github.com/kworkflow/kworkflow/commit/328449c949d5a7e71baa4bdec4ca326ebcbc77c1">here</a>.</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about our first contribution to kernel workflow]]></summary></entry><entry><title type="html">Understanding the Debian packaging system (or at least trying to) [Part 1]</title><link href="https://luizabrs.github.io/blog/2024/debianpackage/" rel="alternate" type="text/html" title="Understanding the Debian packaging system (or at least trying to) [Part 1]"/><published>2024-06-04T07:14:00+00:00</published><updated>2024-06-04T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/debianpackage</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/debianpackage/"><![CDATA[<p>This is another post of the series of contributions and experiences through the discipline of MAC0470 (Free Software Development).</p> <p>First things first, let’s talk about Debian:</p> <h2 id="what-is-debian">What is <code class="language-plaintext highlighter-rouge">Debian</code>?</h2> <p>Debian is a free and open-source Linux distribution renowned for its stability, security, and extensive software repository. It is actively developed through a community-driven workflow, following many open-source principles.</p> <p>Debian’s package management system is defined by <code class="language-plaintext highlighter-rouge">apt</code>, and it simplifies software installation and updates. Debian is the foundation for many other distributions, including Ubuntu, and it supports a wide range of hardware architectures.</p> <h3 id="ok-but-what-is-a-linux-distribution">Ok, but what is a Linux distribution?</h3> <p>A Linux distribution is an operating system built on the Linux kernel, combined with system software and libraries. Each distribution typically includes a package management system to handle software installation and updates.</p> <p>Distributions vary in focus, such as user-friendliness, security, or performance, catering to different user needs. Examples include Ubuntu, Fedora, Debian, Manjaro, Kali and each of them has unique features and tools. Linux distributions are widely used in servers, desktops, and embedded systems due to their flexibility and open-source nature.</p> <h3 id="and-whats-the-meaning-of-apt">And what’s the meaning of <code class="language-plaintext highlighter-rouge">apt</code>?</h3> <p>Debian <code class="language-plaintext highlighter-rouge">apt</code>, short for Advanced Package Tool is a package management system used by Debian and its derivatives to handle the installation, update, and removal of software packages.</p> <p><code class="language-plaintext highlighter-rouge">apt</code> simplifies all the instalation tree by searching for dependencies and retrieving packages from repositories. It uses various command-line tools, such as <code class="language-plaintext highlighter-rouge">apt-get</code> and <code class="language-plaintext highlighter-rouge">apt</code>. Ah, and just for record a doubt that I had for a long time: <code class="language-plaintext highlighter-rouge">apt</code> is intended to simplify package management for users, while <code class="language-plaintext highlighter-rouge">apt-get</code> is more suited for scripting and detailed package management tasks, in other words, <code class="language-plaintext highlighter-rouge">apt</code> is a cuter version of <code class="language-plaintext highlighter-rouge">apt-get</code>.</p> <h2 id="contributing-with-packages-for-debian">Contributing with packages for Debian</h2> <p>Alike many other open-source projects, cntributing with packages to Debian helps support the open-source community and enhances the software ecosystem.</p> <p>It involves understanding Debian’s policies, setting up a packaging environment, and learning the packaging process. Contributors maintain and update software, ensuring its reliability and availability.</p> <p>This makes it easy to understand why contributing to the project is something significant. Debian and its derivatives, such as Ubuntu and Linux Mint, are widely used in commercial fields and backend servers, resulting in a significant role in our lives today, serving as the foundation for many systems.</p> <p>To contribute, if you have software that you’d like to make more accessible to others, that’s an excellent starting point. The next step involves learning how to create and publish this package so it can be downloaded via <code class="language-plaintext highlighter-rouge">apt</code>, which we should explore further.</p> <h3 id="how-to-contribute">How to contribute?</h3> <p>For this project we will be using <strong>Joenio Marques da Costa</strong> tutorials (in portuguese):</p> <ul> <li><a href="https://joenio.me/tutorial-pacote-debian-parte1/">Parte 1: Tutorial de empacotamento Debian</a></li> <li><a href="https://joenio.me/tutorial-pacote-debian-parte2/">Parte 2: Criando o seu próprio pacote Debian</a></li> </ul> <h2 id="installing-debian-testing-via-docker">Installing Debian Testing via Docker</h2> <p>The recommended version of Debian to work with packages is the Testing version. It has a nice balance between Unstable and Stable.</p> <p>Considering I was having QEMU issues, I prefered using the Docker alternative to proceed with this. I followed the tutorial given by the TA.</p> <p>First, follow these command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>project_name
<span class="nb">cd </span>project_name
<span class="nb">sudo </span>docker run <span class="nt">--privileged</span> <span class="nt">--name</span> debian2 <span class="nt">-v</span> .:/app:Z <span class="nt">-it</span> debian
</code></pre></div></div> <p>Inside the container, the <code class="language-plaintext highlighter-rouge">/app</code> directory will be a direct link to the directory your terminal was in when you ran docker run.</p> <p>The creation command mounts the project_name directory to the <code class="language-plaintext highlighter-rouge">/app</code>directory inside the container. To maintain your files, use the <code class="language-plaintext highlighter-rouge">/app</code>directory within the container.</p> <p>To reconnect to the container if it is still running:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker <span class="nb">exec</span> <span class="nt">-it</span> debian2 /bin/bash
</code></pre></div></div> <p>If it is not running but has not been deleted, start it with the following command (then use the previous command to connect):</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker start debian2
</code></pre></div></div> <p>And to stop:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>docker stop debian2
</code></pre></div></div> <h2 id="configuring-a-development-environment-for-debian-packages-inside-the-docker">Configuring a development environment for Debian Packages inside the docker</h2> <p>This part works exactly like <strong>Joenio</strong> tutorial suggests.</p> <p>With Debian Testing running in a Docker container, we start by preparing the environment. First, ensure <code class="language-plaintext highlighter-rouge">ssh-server</code> is enabled and running in the container:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>apt upgrade
<span class="nb">sudo </span>apt <span class="nb">install </span>openssh-server
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>ssh
<span class="nb">sudo </span>systemctl start ssh
</code></pre></div></div> <p>To find the container’s IP address, use <code class="language-plaintext highlighter-rouge">ip a</code> in the command line. Now, access the container via SSH:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh &lt;username&gt;@&lt;container-IP-address&gt;
</code></pre></div></div> <p>Type <code class="language-plaintext highlighter-rouge">yes</code> in the terminal and press <code class="language-plaintext highlighter-rouge">Enter</code>.</p> <p>Next, install useful development tools for packaging:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>devscripts debhelper debian-policy git-buildpackage pkg-perl-tools
</code></pre></div></div> <p>Also, install <code class="language-plaintext highlighter-rouge">apt-file</code> and update the database for <code class="language-plaintext highlighter-rouge">dh-make-perl</code> to find Debian packages referred by Perl libraries:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>apt-file
<span class="nb">sudo </span>apt-file update
</code></pre></div></div> <p>Now, configure your Git for meaningful commits:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.email <span class="s2">"you@example.com"</span>
git config <span class="nt">--global</span> user.name <span class="s2">"Your Name"</span>
</code></pre></div></div> <p>Note that <code class="language-plaintext highlighter-rouge">dh-make-perl</code> relies on variables for <code class="language-plaintext highlighter-rouge">debian/</code> directory data:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">EMAIL</span><span class="o">=</span>you@example.com
<span class="nb">export </span><span class="nv">DEBFULLNAME</span><span class="o">=</span><span class="s2">"Your Name"</span>
</code></pre></div></div> <p>You can add these lines to <code class="language-plaintext highlighter-rouge">~/.bash_profile</code> for automatic setup on system restarts.</p> <p>If it’s your first time using <code class="language-plaintext highlighter-rouge">CPAN</code>, run this command before <code class="language-plaintext highlighter-rouge">dh-make-perl</code>:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cpan
</code></pre></div></div> <h3 id="a-random-question-why-perl">A random question: why Perl?</h3> <p>Debian packages are not exclusively created using Perl, but Perl plays a significant role in the Debian packaging ecosystem due to several historical and practical reasons.</p> <p>Perl’s strong text-processing capabilities make it ideal for tasks such as parsing configuration files, generating package metadata, and handling various text-based formats involved in packaging.</p> <p>Many essential Debian packaging tools, like <code class="language-plaintext highlighter-rouge">debconf</code> for handling configuration questions, <code class="language-plaintext highlighter-rouge">lintian</code> for checking common packaging errors, and <code class="language-plaintext highlighter-rouge">debhelper</code> for assisting with package building, are written in Perl. Note that this historical context and the comprehensive text-processing strengths of Perl have cemented its place in the Debian infrastructure.</p> <p>Moreover, the Comprehensive Perl Archive Network, long for <code class="language-plaintext highlighter-rouge">cpan</code> has contributed significantly to Perl’s prominence in Debian packaging.</p> <p><code class="language-plaintext highlighter-rouge">cpan</code> is a vast repository of Perl modules, and tools like <code class="language-plaintext highlighter-rouge">dh-make-perl</code> facilitate the creation of Debian packages from these modules, streamlining the packaging process for Perl software.</p> <p>Over time, a strong tradition of using Perl for Debian packaging tools has developed, resulting in a wealth of Perl expertise within the Debian community. This tradition, combined with the practical advantages Perl offers, has led to its widespread use and continued importance in the Debian packaging ecosystem.</p> <p><strong>P.S.</strong></p> <p>Here is the exact reason my root was full on the previous post. It was while I was wrongly (focus on <strong>wrongly</strong>) installing perl on my Ubuntu and not on the VM or Docker enviroment. I found out by experience that Perl is extremely heavy.</p> <p>Perl is considered heavy due to its extensive standard library, legacy code for backward compatibility, and dynamic features that increase memory and CPU usage.</p> <p>Also, the interpreter’s overhead and complex syntax also contribute to its perceived bloat.</p> <h2 id="choosing-a-library-and-moving-on">Choosing a library and moving on…</h2> <p>Out of a list of available libraries, we chose <code class="language-plaintext highlighter-rouge">Hash::Wrap</code>.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dh-make-perl locate Hash::Wrap
<span class="o">==</span> dh-make-perl 0.124 <span class="o">==</span>
Parsing Contents files:
	ll.lz4
	md64.lz4
Hash::Wrap is not found <span class="k">in </span>any Debian package
</code></pre></div></div> <p>Here’s where we start to branch off from the tutorial. Instead of using the <code class="language-plaintext highlighter-rouge">Hello World</code> library, which doesn’t offer much practical use beyond teaching the structure of Debian packages, and well… It’s <code class="language-plaintext highlighter-rouge">Hello World</code> we’ll pick something more useful.</p> <p>Make sure to choose a library that isn’t already a Debian package. By the time you’re reading this, the library mentioned might already be packaged, so you’ll need to find another one to work with.</p> <p>But don’t worry, you can use any library you’re interested in to create Debian packages. Don’t feel limited by the few options shown in the tutorial.</p> <p>Next, we’ll create the first version of our package. Run the following commands to download and install the code from the upstream source and set up some template files inside the <code class="language-plaintext highlighter-rouge">debian/</code> directory:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>hash_wrap_pack <span class="o">&amp;&amp;</span> <span class="nb">cd </span>hash_wrap_pack
dh-make-perl <span class="nt">--pkg-perl</span> <span class="nt">--cpan</span> Hash::Wrap
</code></pre></div></div> <p>This created a new directory, along with a <code class="language-plaintext highlighter-rouge">tar</code> file, named <code class="language-plaintext highlighter-rouge">libhash-wrap-perl</code>. You can just <code class="language-plaintext highlighter-rouge">cd</code> into this directory and start working with the package.</p> <p>The next steps for this can be found in part 2…</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about the debian package system and my experience trying to contribute to the project.]]></summary></entry><entry><title type="html">Understanding the Debian packaging system (or at least trying to) [Part 2]</title><link href="https://luizabrs.github.io/blog/2024/debianpackage2/" rel="alternate" type="text/html" title="Understanding the Debian packaging system (or at least trying to) [Part 2]"/><published>2024-06-04T07:14:00+00:00</published><updated>2024-06-04T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/debianpackage2</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/debianpackage2/"><![CDATA[<p>This is another post of the series of contributions and experiences through the discipline of MAC0470 (Free Software Development).</p> <p>Now that we have chosen the package that we are contributing, lets move to:</p> <h2 id="first-base-changes">First base changes:</h2> <p>On the development of changing the <code class="language-plaintext highlighter-rouge">Hash::Wrap</code> package we had to check the files inside <code class="language-plaintext highlighter-rouge">debian/</code>:</p> <ul> <li>copyright</li> <li>control</li> <li>changelog</li> </ul> <h3 id="debiancopyright"><code class="language-plaintext highlighter-rouge">debian/copyright</code>:</h3> <p>To check for the copyrights of the packages, use the following commands:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">grep</span> <span class="nt">-i</span> <span class="nt">-r</span> <span class="nt">--exclude-dir</span><span class="o">=</span>.git <span class="nt">--exclude-dir</span><span class="o">=</span>debian copyright
<span class="nv">$ </span>licensecheck <span class="nt">--shortname-scheme</span><span class="o">=</span>debian,spdx <span class="nt">--copyright</span> <span class="nt">--recursive</span> <span class="nb">.</span>
<span class="nv">$ </span>scan-copyrights
</code></pre></div></div> <p>These commands will help you locate the copyright information without manually checking all the files in the repository. After running these commands, we discovered that the copyright information was incomplete, and some reorganization of the files is necessary.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/debian_copyright-480.webp 480w,/assets/img/debian_copyright-800.webp 800w,/assets/img/debian_copyright-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/debian_copyright.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Also, can remove the DISCLAIMER message, then commit these changes and you are good to go.</p> <h3 id="debiancontrol"><code class="language-plaintext highlighter-rouge">debian/control</code>:</h3> <p>At the time this blog was produced, the policy for Standards-Version in the <code class="language-plaintext highlighter-rouge">control</code> file should be 4.7.0, and the version for <code class="language-plaintext highlighter-rouge">debhelper-compat</code> should be 13. ` However, these changes were not necessary because the base configuration from <code class="language-plaintext highlighter-rouge">dh-make-perl</code> did not alter these settings. Keep in mind that you might need to update them for other packages.</p> <p>You can check the versions of <code class="language-plaintext highlighter-rouge">Standards-Version</code> and <code class="language-plaintext highlighter-rouge">debhelper-compat</code> with the following commands:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Copy code
<span class="nv">$ </span>dpkg-query <span class="nt">--show</span> <span class="nt">--showformat</span> <span class="s1">'${Version}\n'</span> debian-policy
<span class="nv">$ </span>rmadison <span class="nt">--suite</span><span class="o">=</span>unstable debhelper | <span class="nb">cut</span> <span class="nt">-d</span><span class="s2">"|"</span> <span class="nt">-f</span> 2 | <span class="nb">sed</span> <span class="s1">'s/\s\+//'</span>
</code></pre></div></div> <p>Often, you will need to update the <em>Description</em> because <code class="language-plaintext highlighter-rouge">dh-make-perl</code> uses the upstream description, which is usually not adequate. It is likely you will need to change it to something more meaningful.</p> <p>The final result of our changes was this:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/debian_control-480.webp 480w,/assets/img/debian_control-800.webp 800w,/assets/img/debian_control-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/debian_control.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Verify if everything is correct with this file using:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>cme check dpkg-control debian/control
Reading package lists... Done
Building dependency tree... Done
Reading state information... Done
</code></pre></div></div> <p>If no further changes are needed, you can commit the file and proceed with the following steps.</p> <h3 id="debianchangelog"><code class="language-plaintext highlighter-rouge">debian/changelog</code>:</h3> <p>Before modifying this file, we need to ensure our package is correctly set up. Build it and run <code class="language-plaintext highlighter-rouge">lintian</code> to check for any issues.</p> <p>We encountered an issue where some environment PERL variables were incorrectly configured, causing package installation paths to point to directories within the user’s <code class="language-plaintext highlighter-rouge">/home</code>, which is not allowed for Debian packages.</p> <p>To fix this, start a clean environment using <code class="language-plaintext highlighter-rouge">env -i</code> and run the following commands:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">env</span> <span class="nt">-i</span> <span class="nb">sudo </span>pbuilder create
<span class="nv">$ </span><span class="nb">env</span> <span class="nt">-i</span> <span class="nb">sudo </span>pbuilder update
</code></pre></div></div> <p>These commands will build and update the package. Next, ensure all dependencies are installed by running:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">env</span> <span class="nt">-i</span> <span class="nv">BUILDER</span><span class="o">=</span>pbuilder git-pbuilder
</code></pre></div></div> <p>Then, check the package with <code class="language-plaintext highlighter-rouge">lintian</code>:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>lintian <span class="nt">-I</span> <span class="nt">--profile</span> pkg-perl <span class="nt">--info</span>
</code></pre></div></div> <p>For this package, we received a warning: <code class="language-plaintext highlighter-rouge">initial-upload-closes-no-bugs</code>. We’ll fix this by opening an ITP report and updating our changelog file.</p> <p>Now, run development tests to ensure our package passes:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>ubuntu-dev-tools <span class="c"># Contains dev tests</span>
<span class="nv">$ </span><span class="nb">sudo </span>adduser &lt;username&gt; sbuild        <span class="c"># Allows you to run without root</span>
<span class="nv">$ </span>mk-sbuild sid                         <span class="c"># Creates a chroot i.e. sid-amd64</span>
<span class="nv">$ </span>autopkgtest <span class="nb">.</span> <span class="nt">--</span> schroot sid-amd64    <span class="c"># Builds and tests with the chroot created</span>
</code></pre></div></div> <p>Check the last lines for confirmation:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>All tests successful.
...
autodep8-perl-recommends PASS <span class="o">(</span>superficial<span class="o">)</span>
</code></pre></div></div> <p>This indicates our package is ready for upload. However, we need to open an ITP report and update our changelog first.</p> <p>To open an ITP report, send an email to <code class="language-plaintext highlighter-rouge">submit@bugs.debian.org</code>. Generate the subject and content by running:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>dpt gen-itp <span class="o">&gt;</span> /tmp/mail
<span class="nv">$ </span><span class="nb">cat</span> /tmp/mail
</code></pre></div></div> <p>You can use any e-mail to send this.</p> <p>Once the buf report is created we are good to follow. Our bug report can be found <a href="https://bugs.debian.org/cgi-bin/bugreport.cgi?bug=1071516">here</a>.</p> <p>With this task done, we can update our <code class="language-plaintext highlighter-rouge">debian/changelog</code> file:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>dch <span class="nt">--closes</span> 1071516 <span class="c"># Updates changelog message to close this bug</span>
<span class="nv">$ </span>dch <span class="nt">--release</span>        <span class="c"># Switch 'unreleased' to 'unstable' status</span>
</code></pre></div></div> <p>We can now commit and move on.</p> <h2 id="preparations-to-upload-the-package">Preparations to upload the package</h2> <p>To move on, first, create a Salsa account. A moderator will review your registration and accept it. This process can take a few hours or even days.</p> <p>Than, it is necessary to associate an SSH Key with Your Salsa Profile. Once your account is accepted, you need to associate an SSH key with your profile on Salsa Gitlab. The SSH key must be configured on the machine you’re working with, which is the Virtual Machine (VM) for this package.</p> <h3 id="steps-to-set-up-an-ssh-key-in-the-debian-vm">Steps to Set Up an SSH Key in the Debian VM</h3> <ol> <li><strong>Generate the SSH Key Using the ed25519 Algorithm</strong>: <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"your_email@example.com"</span>
</code></pre></div> </div> </li> <li><strong>Start the ssh-agent in the Background</strong>: <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span><span class="nb">eval</span> <span class="s2">"</span><span class="si">$(</span>ssh-agent <span class="nt">-s</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div> </div> </li> <li><strong>Add Your Private Key to the Agent</strong>: <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="nv">$ </span>ssh-add ~/.ssh/id_ed25519
</code></pre></div> </div> </li> <li><strong>Copy the Public Key to Salsa Gitlab</strong>: <ul> <li>Run the following command to get your public key: <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> ~/.ssh/id_ed25519.pub
</code></pre></div> </div> </li> <li>Copy and paste the content of your public key into Salsa Gitlab when associating a new SSH key to your profile. Ensure you use the public key (<code class="language-plaintext highlighter-rouge">id_ed25519.pub</code>), not the private one.</li> </ul> </li> </ol> <p>After completing these steps, you can upload packages to Salsa Gitlab without any issues or additional authentication.</p> <h2 id="uploading-the-package-to-debian-perls-team-repository-incomplete">Uploading the package to Debian Perl’s team repository (incomplete)</h2> <p>First, we need to join the Perl Team. To do this, send them an email with a brief introduction and your Salsa username. You can refer to the email I sent <a href="https://lists.debian.org/debian-perl/2024/06/msg00000.html">here</a>.</p> <p>However, we encountered difficulties in reaching the Perl Team, as they did not respond to any emails from our MAC0470 group.</p> <p>We have uploaded our progress with this package on Salsa Gitlab: <a href="https://salsa.debian.org/lincolnyuji/hash-wrap-package">Hash Wrap on Salsa</a>.</p> <p>We will contact Joenio, who authored the tutorials we used, to help finalize the job and upload the package to the Perl Team.</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about the debian package system and my experience trying to contribute to the project.]]></summary></entry><entry><title type="html">When your root has a total of ZERO available space</title><link href="https://luizabrs.github.io/blog/2024/root/" rel="alternate" type="text/html" title="When your root has a total of ZERO available space"/><published>2024-05-15T07:14:00+00:00</published><updated>2024-05-15T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/root</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/root/"><![CDATA[<p>This week a new issue has popped on my laptop:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/error-480.webp 480w,/assets/img/error-800.webp 800w,/assets/img/error-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/error.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Of course my first instinct was just to press <del>ignore</del> :grimacing:</p> <p>But obviously I wanted to turn my laptop off and be able to turn it on again, so I <strong>need</strong> to come up with a solution.</p> <h2 id="an-awful-idea">An awful idea</h2> <p>An awful idea was just running <code class="language-plaintext highlighter-rouge">sudo apt clean</code> and <code class="language-plaintext highlighter-rouge">sudo apt autoremove</code>.</p> <h3 id="what-does-sudo-apt-clean-do">What does <code class="language-plaintext highlighter-rouge">sudo apt clean</code> do?</h3> <p>So, we constatly install software and applications on the fly, so, when the necessity or a dependency asks, you simply go and <code class="language-plaintext highlighter-rouge">sudo apt instal &lt;something here&gt;</code>. This seems ok, until when each installation leaves behind a huuuuuge trail of cached package files, quietly consuming precious disk space.</p> <p>This is where “sudo apt-get clean” steps in, it basically frees our system from the random and useless cached package files (most of them came from software installations via the “apt-get” package manager). What happens is that sometimes these packages are useful on that moment, but later it becomes a pointless “clog of space”.</p> <p>By executing the “clean” command, we are effectively hitting the reset button on the cache (Goobye Cache! :smirk:). This isn’t just about tidying up; it’s about reclaiming valuable disk space for our system to breathe easy once again.</p> <p>What is cool about <code class="language-plaintext highlighter-rouge">sudo apt clean</code> is that it targets only cached package files, leaving your installed packages untouched and your system functionality intact (Yays, things will keep working :relieved:).</p> <h3 id="what-does-sudo-apt-autoremove-do">What does <code class="language-plaintext highlighter-rouge">sudo apt autoremove</code> do?</h3> <p>Again, we are constantly installing and uninstalling various software packages to suit our needs. Simultaniously, our system accumulates dependencies – those extra stuff that were necessary for a particular application but are no longer needed once this application is removed.</p> <p>The problem is that these dependencies are pretty much like digital ghosts, taking up valuable disk space and cluttering your system with unnecessary files.</p> <p>This is where <code class="language-plaintext highlighter-rouge">sudo apt autoremove</code> save the day (and most of the time it does :ok_hand:). This command scans the system for orphaned dependencies – those no longer required by any installed package – and just :wave: remove them.</p> <p>What makes <code class="language-plaintext highlighter-rouge">autoremove</code> more complete than <code class="language-plaintext highlighter-rouge">clean</code> is that it is very precise on what it is removing. It prunes away only those dependencies that are truly superfluous, leaving the system lighter without sacrificing stability or performance.</p> <h3 id="why-awful">Why awful?</h3> <p>They seem good, until you know they just remove something around 300MB of memory :disappointed:</p> <h2 id="a-good-idea">A good idea</h2> <p>After googling a bit, this <a href="https://askubuntu.com/questions/73867/the-volume-filesystem-root-has-only-0-bytes-disk-space-remaining">cool post</a> on <strong>ask ubuntu</strong> brought some good insights on “superfluous” directories content that I could somehow get rid of.</p> <p>So, I essentially switched to the root folder <code class="language-plaintext highlighter-rouge">/</code> and executed the <code class="language-plaintext highlighter-rouge">sudo rm -r \path\to\dir\*</code>.</p> <h3 id="what-does-sudo-rm-do">What does <code class="language-plaintext highlighter-rouge">sudo rm</code> do?</h3> <p>I could be super quick and just explain that <code class="language-plaintext highlighter-rouge">sudo rm</code> is a simple instruction to delete files and directories and thats it. But there is a lot more…</p> <p>With <code class="language-plaintext highlighter-rouge">sudo rm</code>, you can delete files that resist deletion under normal circumstances with just a snap. But with this power comes a significant risk. One misplaced character, one mistyped path, and you could can erase crucial system files or wipe out entire directories of irreplaceable data. (Yes, we are talking about reinstalling your linux from scratch :trollface:)</p> <p>The lesson here is clear: <strong>ALWAYS</strong> be careful with <code class="language-plaintext highlighter-rouge">sudo rm</code>. Always double-check your command before hitting <code class="language-plaintext highlighter-rouge">Enter</code>. Maybe you can consider implementing safeguards like confirmation prompts or regular backups to mitigate the risk of irreversible mistakes. (Are you sure that you are sure? :loudspeaker::loudspeaker::loudspeaker:)</p> <h3 id="dont-delete-what-you-dont-know-what-it-is">Don’t delete what you don’t know what it is!</h3> <p>So, It is extremely important to also know what are we actually deleting. This part I will only list the directories I actually cleaned.</p> <ul> <li><code class="language-plaintext highlighter-rouge">/tmp</code> : A temporary directory for storing short-lived files, often used by programs for cache and intermediate data. Files here are typically deleted automatically by the system or upon reboot, providing transient storage. Users and programs have write access for creating temporary files. (r like the post says: user temp data – these are commonly all deleted every reboot anyway)</li> <li><code class="language-plaintext highlighter-rouge">/var/tmp</code> : Similar to <code class="language-plaintext highlighter-rouge">/tmp</code>, but files here persist across reboots, offering longer-term temporary storage. Used for larger or longer-lasting temporary files that need to survive system restarts. Users and programs also have write access, and files are retained until manually deleted. (or like the post says : print spools, and other system temporary data)</li> <li><code class="language-plaintext highlighter-rouge">/opt</code> : Directory for optional or add-on software installations, separate from the core system files. Provides a standardized location for third-party software, maintaining system organization. Typically requires root privileges for installation, offering a dedicated space for non-native applications. (or like the post says: many third-party apps install here, and don’t clean up after themselves)</li> <li><code class="language-plaintext highlighter-rouge">/var/log</code> : Repository for system log files containing records of system events, errors, and activity. Organized by category or application, aiding in system troubleshooting and monitoring. Readable by users for system insight, with write access restricted to system processes or privileged users. (or like the post says: log files can eat up a lot of space if there are repetitive errors )</li> </ul> <h2 id="a-perfect-idea">A perfect idea</h2> <p>Ok, the problem is that all the two previous solutions are temporary and don’t actually solve whatever is consuming the root, or even correct the root size itself.</p> <p>Myself, I hold a dual boot (Windows + Ubuntu) and when I did my installation I messed up with the sizes of the partitions.</p> <p>Having said that, the ideal (almost perfect) solution would be choosing a distro and boot from a LiveCD (or pendrive). After this, run the <code class="language-plaintext highlighter-rouge">gparted</code> to resize and correct the partitions.</p> <p>That’s all for this post! :smiley:</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about my struggle of facing my root not having any space and what I did to (try) to fix it]]></summary></entry><entry><title type="html">Update - Road to making our first patch to linux iio (patch accepted)</title><link href="https://luizabrs.github.io/blog/2024/acceptedpatch/" rel="alternate" type="text/html" title="Update - Road to making our first patch to linux iio (patch accepted)"/><published>2024-05-07T10:45:00+00:00</published><updated>2024-05-07T10:45:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/acceptedpatch</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/acceptedpatch/"><![CDATA[<p>This is a post update about our <a href="https://luizabrs.github.io/blog/2024/patch/">first</a> patch to linux iio.</p> <h2 id="patch-update">Patch update:</h2> <p>Our patch was finally accepted after a long mailing list.</p> <p>You can check it out <a href="https://lore.kernel.org/all/20240429132233.6266-1-lincolnyuji@usp.br/">here</a>!</p> <h2 id="patch-uptade-2">Patch uptade 2:</h2> <p>Our patch is now part of the latest major kernel release.</p> <p>They added the patch titled</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iio: adc: ti-ads1015: use device_for_each_child_node_scoped<span class="o">()</span>
</code></pre></div></div> <p>to my char-misc git tree which can be found <a href="git://git.kernel.org/pub/scm/linux/kernel/git/gregkh/char-misc.git">here</a> in the char-misc-next branch.</p> <p>:smiley:</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This is a post about our first contribution to the linux community.]]></summary></entry><entry><title type="html">Contributing to kw(kernel workflow)</title><link href="https://luizabrs.github.io/blog/2024/kw/" rel="alternate" type="text/html" title="Contributing to kw(kernel workflow)"/><published>2024-05-06T07:14:00+00:00</published><updated>2024-05-06T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/kw</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/kw/"><![CDATA[<p>This is another post of the series of contributions and experiences through the discipline of MAC0470 (Free Software Development).</p> <h2 id="what-is-kw-or-simply-kworkflow">What is <code class="language-plaintext highlighter-rouge">kw</code> or simply kworkflow?</h2> <p>In a very simple way, just like the <a href="https://kworkflow.org/">website</a> suggests: <code class="language-plaintext highlighter-rouge">kw</code>has a simple mission - reduce the environment and setup overhead of developing for Linux.</p> <p>The tool is made of different components and assists developers working on the Linux kernel. It helps and automates the working flow of developers working on the Linux Kernel.</p> <p>The coolest part is that a few of the maintainers of the projects are members of the university of são paulo community, which makes any doubts or issues we have quickly solved.</p> <p>You can check the documentation <a href="https://github.com/kworkflow/kworkflow">here</a>.</p> <h2 id="finding-an-issue-to-contribute---a-good-first-issue-smiley">Finding an issue to contribute - a good first issue :smiley:</h2> <p>If you look up on the documentation mentioned above, you will find more than 150 <a href="https://github.com/kworkflow/kworkflow/issues">issues</a> opened by contributors that can (or need) to be solved.</p> <p>Scrolling around, you can find the ones labelled as <code class="language-plaintext highlighter-rouge">good first issue</code>, which helped us find <code class="language-plaintext highlighter-rouge">issue #69</code> to be the one to contribute.</p> <h3 id="issue-69--improve-kw-m-authors-for-multiline-module_author-statements">Issue #69 : Improve “kw m –authors” for multiline MODULE_AUTHOR statements</h3> <p>On the <a href="https://github.com/kworkflow/kworkflow/issues/69">documentation</a> we understood that the task was improoving the function responsible to capture and print the authors of the drivers through the macro <code class="language-plaintext highlighter-rouge">MODULE_AUTHORS</code>.</p> <p>However, the old implementation of this function can only do this for single-line statements of this macro. If authors are defined in multi-line statements, no author will be captured by the function.</p> <p>Or as the issue author of the issue mentioned:</p> <blockquote> <p>When we run a kw m –authors, only authors found in a single line MODULE_AUTHOR statement are captured and printed. In the future, it would be nice to look for multiline MODULE_AUTHOR statements such as:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MODULE_AUTHOR ("a_long_email@xx.com" \
               "another_long@yy.com" )
</code></pre></div> </div> <p>and:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MODULE_AUTHOR ("a_long_email@xx.com"
               "another_long@yy.com" )
</code></pre></div> </div> <p>which are, both, valid C statements. In the currently implementation neither of these four emails would be printed.</p> </blockquote> <h2 id="contributing-to-kw">Contributing to <code class="language-plaintext highlighter-rouge">kw</code></h2> <p>This part of the post was based on the TA’s pad about <code class="language-plaintext highlighter-rouge">kw-contribution-guide</code>.</p> <p><strong><span style="color:#C61AAE;">1) Forking kw</span></strong></p> <p>Here we need to create a fork of the <code class="language-plaintext highlighter-rouge">kw</code> before doing any modification (differente from adventurous kernel contribution). We need to access the upstream and <code class="language-plaintext highlighter-rouge">fork</code> the repository. Note that your fork of kw is not automatically synchronized with the upstream when there are updates in the <strong>unstable</strong> branch. To do this, in your fork, select <strong>unstable</strong> and click on <code class="language-plaintext highlighter-rouge">Sync fork</code>.</p> <p><strong><span style="color:#C61AAE;">2) Cloning the repo locally</span></strong></p> <p>After setting up you ssh key on Github, you can use the following command to <code class="language-plaintext highlighter-rouge">git clone</code> the original repository</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git clone git@github.com:&lt;nome-de-usuario&gt;/kworkflow.git
</code></pre></div></div> <p><strong><span style="color:#C61AAE;">3) Using the unstable branch</span></strong></p> <p>In this project we will any change on a branch called <code class="language-plaintext highlighter-rouge">unstable</code> inside the cloned repository.</p> <p>You can run:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git switch unstable
</code></pre></div></div> <p>or (if the previous didn’t work out):</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git checkout <span class="nt">--track</span> origin/unstable
</code></pre></div></div> <p>You can ask yourself, but what is an unstable brach? An “unstable branch” ia a branch in Git, where ongoing development work is carried out. In other words, it is the “lab” for developers experiments. This branch often contains the latest changes, features, and bug fixes that are still under testing and may not be considered stable for production use. Ideally, after issues fixed and testings are correctly done, this branch is merged to the origin for release.</p> <p><strong><span style="color:#C61AAE;">4) <code class="language-plaintext highlighter-rouge">kw</code> installation</span></strong></p> <p>Now, with the right repository set, just go to the rpository root and run</p> <ul> <li><code class="language-plaintext highlighter-rouge">./setup.sh --install</code></li> <li>type <code class="language-plaintext highlighter-rouge">y</code> and <code class="language-plaintext highlighter-rouge">enter</code> to install all dependencies</li> <li>if you want to install without the man pages (quicker :sunglasses: ), run <code class="language-plaintext highlighter-rouge">./setup.sh --install --skip-docs</code></li> </ul> <p><strong><span style="color:#C61AAE;">5) Developer dependencies installation</span></strong></p> <p>For the development tools, there are a few needed dependencies:</p> <p><strong>Unit Test</strong></p> <p>We use <strong>shUnit2</strong> as the unit testing framework. To run the tests, you can clone the shUnit2 repository <a href="https://github.com/kward/shunit2">here</a> and add the path to this repository in your PATH. Alternatively, you can clone the repository into the <code class="language-plaintext highlighter-rouge">tests/</code> directory within your local <code class="language-plaintext highlighter-rouge">kw</code> repository (easier).</p> <p><strong>Formatter, Linter, and pre-commit</strong>:</p> <p>We use <strong>shfmt</strong> as the formatter, <strong>shellcheck</strong> as the linter, and <strong>pre-commit</strong> for pre-commit hooks. These tools are used to ensure that general coding style rules are followed. After installing them with your package manager, run</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pre-commit install
</code></pre></div></div> <p>in the root of the <code class="language-plaintext highlighter-rouge">kw</code> repository. From there, every time you make a commit, the pre-commit hook will check if your commit follows the general coding style rules. Note that passing the pre-commit does not guarantee that your commit follows all coding style rules, but it greatly speeds up the development process.</p> <p><strong>A note about the tools:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">shfmt</code>: It is used as a formatter, meaning it helps ensure that shell scripts are formatted consistently according to predefined style guidelines. This improves code readability and maintainability.</li> <li><code class="language-plaintext highlighter-rouge">shellcheck</code>: This tool serves as a linter, which means it analyzes shell scripts statically to detect and highlight potential issues, errors, or code smells. It helps developers identify and correct problems early in the development process.</li> <li><code class="language-plaintext highlighter-rouge">pre-commit</code>: It is a tool used to set up and manage pre-commit hooks, which are scripts or commands that are run automatically before each commit is made to a version control system. These hooks can perform various tasks such as running tests, checking coding style, or performing static analysis to ensure code quality before changes are committed.</li> </ul> <h2 id="development-flow">Development flow</h2> <p><strong><span style="color:#C61AAE;">1) Update your local <code class="language-plaintext highlighter-rouge">unstable</code> branch with upstream <code class="language-plaintext highlighter-rouge">unstable</code></span></strong></p> <p>If your <code class="language-plaintext highlighter-rouge">kw</code> fork’s <code class="language-plaintext highlighter-rouge">unstable</code> branch isn’t synchronized with the upstream, do this on the Git interface. Afterward, run in your local kw repository:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git pull origin unstable
</code></pre></div></div> <p><strong><span style="color:#C61AAE;">2) Create a dedicated branch for the changes</span></strong></p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git checkout <span class="nt">-b</span> &lt;desired-branch-name&gt;
</code></pre></div></div> <p><strong><span style="color:#C61AAE;">3) Implement the changes - let’s test!</span></strong></p> <p>Regardless of the number of commits, each commit should be self-contained and minimal, meaning the implementation and the testing code should be in the same commit, and the commit shouldn’t “do too much at once”</p> <p>Always run the battery of automated tests to ensure your implementation passes the tests (both existing and new ones). To do this, run:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>./run_tests.sh <span class="nt">--unit</span>
</code></pre></div></div> <p>Don’t underestimate this, unit tests can take a bit too long to finish and depending on the changes you made, it is not necessary to run all the tests all over again. So, you can run a few specific tests with this command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>./run_tests.sh <span class="nb">test </span>tests/unit/&lt;test-script-name&gt;
</code></pre></div></div> <p>For more details on tests, see <a href="https://kworkflow.org/content/tests.html">here</a>.</p> <p><strong><span style="color:#C61AAE;">4) Creating a remote branch on Fork - updating the repo with changes </span></strong></p> <p>After implementing the change, represented by a branch in your local repository, we need to “push” it to your fork using:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git push <span class="nt">--set-upstream</span> origin
</code></pre></div></div> <p>This will add the branch you did your local modification to the remote repository.</p> <p><strong><span style="color:#C61AAE;">5) Open a pull request (PR) on upstream </span></strong></p> <p>After creating the remote branch with the push, open a PR through the Git interface. Typically, a green button labeled “Open Pull Request” appears, and you simply click to open the PR on the upstream. Don’t forget to select unstable as the base branch - Always make sure you are on the right place!</p> <p><strong><span style="color:#C61AAE;">6) Update a PR </span></strong></p> <p>With the reviewing process you will most likely need to do updated on the Pull Request. To do this, you should update the necessary commits with:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git rebase <span class="nt">--interactive</span> unstable
</code></pre></div></div> <p>Don’t forget to mark the commits you want to edit with ‘e’.</p> <p>After updating your local branch, update the remote branch with:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git push <span class="nt">--force-with-lease</span> origin &lt;branch-name&gt;
</code></pre></div></div> <p>The PR on GitHub will be automatically updated afterward.:relieved:</p> <p><strong>A note on git rebase</strong></p> <p>Git <code class="language-plaintext highlighter-rouge">rebase</code> is a command used in Git to integrate changes from one branch onto another by reapplying commits from one branch onto the tip of another. Different from <code class="language-plaintext highlighter-rouge">merge</code>, which preserves the original branching structure, rebasing creates a linear history by moving the entire branch to a new base commit. This process can help maintain a cleaner commit history, making it easier to follow the progression of changes.</p> <p><strong>PR adjustments</strong></p> <p>Before your PR is accepted and merged into the original project, a kw maintainer will review to ensure everything is in order. This includes aspects like commit messages, organization, readability, etc. They may suggest changes before merging your contribution into the unstable branch.</p> <p>The GitHub Actions tests, discussions, and adjustments can be seen on the Pull Request page on GitHub.</p> <h2 id="our-experience-with-the-issue-69-development">Our experience with the issue #69 development:</h2> <p>During the unit tests executionsm we faced the following <code class="language-plaintext highlighter-rouge">ERROR</code></p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Running <span class="nb">test</span> <span class="o">[</span>unit/maintainers_test]
<span class="o">=========================================================</span>
test_print_files_authors
test_print_files_authors_from_dir
ASSERT:expected:&lt;
<span class="o">&gt;</span> <span class="o">=========================================================</span>
<span class="o">&gt;</span> MODULE AUTHORS:
<span class="o">&gt;</span> code1.c: John Doe &lt;johndoe@community.com&gt;, Caesar Doe &lt;caesar@community.com&gt;, Michael Doe &lt;michael@community.com&gt;
<span class="o">&gt;</span> code2.c: Bob Hilson &lt;bob@opensource.com&gt;
<span class="o">&gt;</span> but was:&lt;
<span class="o">&gt;</span> <span class="o">=========================================================</span>
<span class="o">&gt;</span> MODULE AUTHORS:
<span class="o">&gt;</span> code1.c: John Doe &lt;johndoe@community.com&gt;, Caesar Doe &lt;caesar@community.com&gt;, Michael Doe &lt;michael@community.com&gt;
<span class="o">&gt;</span> code2.c: Bob Hilson &lt;bob@opensource.com&gt;
<span class="o">&gt;</span>
shunit2:ERROR test_print_files_authors_from_dir<span class="o">()</span> returned non-zero <span class="k">return </span>code.
test_maintainers_main
test_maintainers_main_patch

Ran 4 tests.

FAILED <span class="o">(</span><span class="nv">failures</span><span class="o">=</span>1<span class="o">)</span>
<span class="o">=========================================================</span>
Total: 1 <span class="nb">test </span>file<span class="o">(</span>s<span class="o">)</span>
1 <span class="nb">test </span>file<span class="o">(</span>s<span class="o">)</span> FAILED

Take a look at:
-&gt; ./tests/unit/maintainers_test.sh
</code></pre></div></div> <p>It was quite easy to fix this error since it was a <code class="language-plaintext highlighter-rouge">\newline</code> specification that made extra gaps on the typing. So the <code class="language-plaintext highlighter-rouge">diff</code> of the test script didn’t embrace the case, even though it seemed “right”.</p> <p>In <a href="https://github.com/kworkflow/kworkflow/pull/1100">this</a> page you can find our PR.</p> <p>Besides this specific error, we are now facing a few issues that the maintainers suggested we modify. We are working on that and we are making the needed changes hoping for new feedbacks.</p> <p>This is it for our first kw contribution :smiley: See you on the next post!</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about our first contribution to kernel workflow]]></summary></entry><entry><title type="html">Road to making our first patch to linux iio</title><link href="https://luizabrs.github.io/blog/2024/patch/" rel="alternate" type="text/html" title="Road to making our first patch to linux iio"/><published>2024-04-18T10:45:00+00:00</published><updated>2024-04-18T10:45:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/patch</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/patch/"><![CDATA[<p>Our group in MAC0470 (Free Software Development) have just finished our first contribution patch to Kernel. In this blog post we will talk about what we did and how our experience was during this process.</p> <h2 id="finding-a-patch-to-contribute">Finding a patch to contribute</h2> <p>There are many things one can do to contribute to the Kernel. But the difficulty level of a contribution can scale from just making naive code-styling corrections or warning suppressions to doing real obscure bugs fixing and new drivers and modules creation.</p> <p>We met in the middle, choosing to use <code class="language-plaintext highlighter-rouge">device_for_each_child_node_scoped()</code> to simplify error handling and avoid bugs. Some drivers iterate over firmware nodes (<code class="language-plaintext highlighter-rouge">struct fwnode_handle</code>) and increment (<code class="language-plaintext highlighter-rouge">_get</code>) a reference for the firmware node while iterating. Ending the loop early (either because it doesn’t need to iterate until the end or due to error) requires the driver to release (<code class="language-plaintext highlighter-rouge">_put</code>) the node reference which is a source of potential bugs if a future patch is not careful about node release. Using <code class="language-plaintext highlighter-rouge">device_for_each_child_node_scoped()</code> helps mitigate such bugs while potentially making error handling code cleaner.</p> <p>In other words, we used the available kernel macro that has the main goal to ease the iteration between firmware nodes by simplifying the error handling in loops early exits and also avoiding possible bugs.</p> <h2 id="what-does-it-mean-to-be-a-scoped-macro">What does it mean to be a “scoped” macro</h2> <p>The macro we are talking about is <code class="language-plaintext highlighter-rouge">evice_for_each_child_node_scoped()</code> which was first introduced on the commit <code class="language-plaintext highlighter-rouge">365130fd47af</code>. This new scoped version removes the necessity of doing manual calls of <code class="language-plaintext highlighter-rouge">fwnode_handle_put()</code> during the code that might leave the loop in advance.</p> <p>Even though this new macro was accepted and introduced to the kernel for quite a long time, there are many drivers that still uses the older version.</p> <p>Given this, it is a good opportunity to refactor the code in a few files to get in touch with the patches submission. Especifically, we modified this driver: <code class="language-plaintext highlighter-rouge">drivers/iio/adc/ti-ads1015.c</code>.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/img2-post1-480.webp 480w,/assets/img/img2-post1-800.webp 800w,/assets/img/img2-post1-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/img2-post1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>As you can observe on the above image, by using this macro, we can remove the <code class="language-plaintext highlighter-rouge">fwnode_handle_put()</code> calls. With this, we are avoiding possible future modification bugs to occur in case any developer forgets to add this call in loops early exits. Standardizing this macro usage, instead of the older version, also makes life easier for the future developer that will work with kernel drivers. :smiley:</p> <h2 id="patch-creation-and-submission">Patch creation and submission</h2> <p>With the proper modifications done, we wrote the following <code class="language-plaintext highlighter-rouge">commit</code> message and properly signed a message as shown below:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/img1-post1-480.webp 480w,/assets/img/img1-post1-800.webp 800w,/assets/img/img1-post1-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/img1-post1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>After that, we used our own git to create and send the patch to the teaching assistants of the discipline</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git format-patch –to<span class="o">=</span>&lt;EMAIL&gt; HEAD~1
<span class="nv">$ </span>git send-email <span class="k">*</span>.patch
</code></pre></div></div> <h2 id="next-step">Next step</h2> <p>After the approval of the TA’s we can then send our contribution to the kernel maintainers.</p> <p>See you on the next post :smiley:</p> <p>Written by Lincoln Yuji, Luiza Soezima and Sabrina Araújo</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This is a post about our first contribution to the linux community.]]></summary></entry><entry><title type="html">How many problems do we need to complete a tutorial</title><link href="https://luizabrs.github.io/blog/2024/first/" rel="alternate" type="text/html" title="How many problems do we need to complete a tutorial"/><published>2024-04-10T07:14:00+00:00</published><updated>2024-04-10T07:14:00+00:00</updated><id>https://luizabrs.github.io/blog/2024/first</id><content type="html" xml:base="https://luizabrs.github.io/blog/2024/first/"><![CDATA[<p>So, for the discipline of MAC0470 (Free Software Development), I was supposed to follow these first tutorials:</p> <ul> <li><a href="https://flusp.ime.usp.br/kernel/qemu-libvirt-setup/">Use QEMU and libvirt to setup a Linux kernel test environment</a></li> <li><a href="https://flusp.ime.usp.br/kernel/build-linux-for-arm/">Build the Linux kernel for ARM</a></li> <li><a href="https://flusp.ime.usp.br/kernel/modules-intro/">Introduction to kernel build configuration and modules</a></li> </ul> <p>And I had several particular struggles with the first and second tutorial. Which drew me back for several times.</p> <p>In this blog post, I will relate my experience while completing the tutorials and how I managed (or not) to go over them.</p> <p>During this phase of the tutorials I faced two major issues: my laptop not being able to keep up with my necessities and way too many new concepts to apply during each step.</p> <h2 id="technical-limitation">Technical limitation</h2> <p>During this phase, I faced the few errors:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reading package lists... Done
Building dependency tree       
Reading state information... Done
E: The package zoom needs to be reinstalled, but I can<span class="s1">'t find an archive for it.
</span></code></pre></div></div> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dpkg: error processing package zoom <span class="o">(</span><span class="nt">--remove</span><span class="o">)</span>:
 package is <span class="k">in </span>a very bad inconsistent state<span class="p">;</span> you should
 reinstall it before attempting a removal
</code></pre></div></div> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPG error: file:/var/cuda-repo-10-2-local-10.2.89-440.33.01 Release: The following signatures couldn<span class="s1">'t be verified because the public key is not available: NO_PUBKEY F60F4B3D7FA2AF80
</span></code></pre></div></div> <p>They all made me unable to finish the tutorials on time, or even finishing them.</p> <p>Especially when we aimed to run the the qemu virtual machine with the debian ISO, my laptop constantly deleted my configs and everytime that I restarted my laptop. Consequently, I had to rerun the entire booting flow for the first time installation of debian everytime I needed to use it, so, in the end, I could not do the linux kernel coding contributions on my machine and had to share with my colleagues.</p> <p>Now, after configuring the system we were able to move on to our first FLOSS contribution :)</p> <p>See you on the next post!</p>]]></content><author><name></name></author><category term="MAC0470"/><category term="FLOSS"/><summary type="html"><![CDATA[This post is about my struggle with understanding the issues while installing qemu, libvirt, linux kernel and other stuff]]></summary></entry></feed>