<div class="card-item col">
  <a href="{% if talk.redirect %}{{ talk.redirect }}{% else %}{{ talk.url | relative_url }}{% endif %}">
    <div class="card hoverable">
      <div class="row g-0">
        {% if talk.img %}
          <div class="card-img col-md-6">
            {% include figure.liquid loading="eager" path=talk.img sizes="(min-width: 768px) 156px, 50vw" alt="talk thumbnail" %}
          </div>
        {% endif %}
        <div class="{% if talk.img %}col-md-6{% else %}col-md-12{% endif %}">
          <div class="card-body">
            <h3 class="card-title text-lowercase">{{ talk.title }}</h3>
            <p class="card-text">{{ talk.description }}</p>
            <div class="row ml-1 mr-1 p-0">
              {% if talk.github %}
                <div class="github-icon">
                  <div class="icon" data-toggle="tooltip" title="Code Repository">
                    <a href="{{ talk.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
                  </div>
                  {% if talk.github_stars %}
                    <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                      <i class="fa-solid fa-star"></i>
                      <span id="{{ talk.github_stars }}-stars"></span>
                    </span>
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
    </div>
  </a>
</div>
